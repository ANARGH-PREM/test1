#include <stdio.h>
void main()
{
	int length,breadth,area=0;
	printf("enter the length");
	scanf("%d",&length);
	print("enter the breadth");
	scanf("%d",&breadth);
	area=length*breadth;
	printf("area is %d",area);
}
